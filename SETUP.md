# 🚀 Быстрый старт

## Шаг 1: Создание бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Придумайте имя (например: "Офисный ниндзя")
4. Придумайте username (например: `office_ninja_game_bot`)
5. **Сохраните токен!** Он выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

## Шаг 2: Деплой мини-приложения на Render

1. Зарегистрируйтесь на [Render.com](https://render.com)
2. Нажмите **New** → **Static Site**
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Name**: `office-ninja-game` (или свое название)
   - **Build Command**: оставьте пустым
   - **Publish Directory**: `.`
5. Нажмите **Create Static Site**
6. Дождитесь деплоя и **скопируйте URL** (например: `https://office-ninja-game.onrender.com`)

## Шаг 3: Настройка Menu Button в боте

1. Вернитесь в [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите своего бота
4. **Bot Settings** → **Menu Button** → **Configure menu button**
5. Введите **URL мини-приложения** с Render
6. Введите текст кнопки: `🎮 Играть`

## Шаг 4: Настройка локального бота

```bash
# 1. Установите Python 3.8+ (если еще не установлен)

# 2. Установите зависимости
pip install -r requirements.txt

# 3. Создайте файл .env
cp env.example .env

# 4. Отредактируйте .env
nano .env  # или используйте любой редактор
```

В файле `.env` укажите:
```
BOT_TOKEN=ВАШ_ТОКЕН_ОТ_BOTFATHER
WEBAPP_URL=https://ваш-адрес.onrender.com
```

## Шаг 5: Запуск бота

### Вариант 1: Простой запуск
```bash
python bot.py
```

### Вариант 2: С помощью скрипта
```bash
./start_bot.sh
```

### Вариант 3: В фоновом режиме (Linux/Mac)
```bash
nohup python bot.py > bot.log 2>&1 &
```

## Шаг 6: Проверка

1. Найдите своего бота в Telegram по username
2. Нажмите **Start** или отправьте `/start`
3. Вы должны увидеть:
   - Картинку с приветствием
   - Текст: "Уничтожь календарь со встречами и вспомни: жизнь есть и вне работы"
   - Кнопку **🎮 Играть**
4. Нажмите кнопку — откроется игра!

## Проверка команд

Отправьте в бота:
- `/start` → приветствие и кнопка игры
- `/who` → `ceo of everything @jypixob`
- `/fuck` → `fuck corporations 🖕`

## ❓ Проблемы?

### Бот не отвечает
- Проверьте, что `bot.py` запущен
- Проверьте правильность токена в `.env`
- Посмотрите логи: `tail -f bot.log`

### Кнопка игры не работает
- Проверьте URL в `.env` (должен совпадать с Render)
- Убедитесь, что мини-приложение задеплоено

### Картинка не показывается
- Убедитесь, что файл `images/start.jpg` существует
- Проверьте путь к файлу в `bot.py`

## 🎉 Готово!

Ваш бот работает! Теперь можно:
- Поделиться ботом с друзьями
- Добавить больше команд
- Кастомизировать сообщения
- Добавить базу данных для сохранения рекордов

---

**Автор**: @jypixob  
**Проект**: Офисный ниндзя 🥷

