# 🎯 НАЧНИТЕ ОТСЮДА

## Что было сделано

Я добавил полноценного **Telegram бота** для вашей игры "Офисный ниндзя" со всеми запрошенными командами! 🎉

## ✅ Реализовано

### 1. Приветствие при первом запуске
Когда пользователь заходит в бота первый раз (команда `/start`):
- ✅ Показывается картинка `images/start.jpg`
- ✅ Текст: "Уничтожь календарь со встречами и вспомни: жизнь есть и вне работы"
- ✅ Кнопка "🎮 Играть" для запуска мини-приложения

### 2. Команда `/start`
- ✅ Выдает первый исходный текст
- ✅ Показывает кнопку для запуска игры
- ✅ Работает при любом повторном вызове

### 3. Команда `/who`
- ✅ Выводит: `ceo of everything @jypixob`

### 4. Команда `/fuck`
- ✅ Выводит: `fuck corporations 🖕`

## 📁 Новые файлы

| Файл | Описание |
|------|----------|
| **bot.py** | Главный файл бота со всеми командами |
| **requirements.txt** | Зависимости Python |
| **env.example** | Пример конфигурации |
| **start_bot.sh** | Скрипт запуска |
| **test_bot.py** | Тестирование настроек |
| **SETUP.md** | Подробная инструкция |
| **QUICKSTART.md** | Быстрый старт |
| **BOT_COMMANDS.md** | Описание команд |

## 🚀 Как запустить (3 шага)

### Шаг 1: Создайте бота
1. Откройте @BotFather в Telegram
2. Отправьте `/newbot`
3. Скопируйте токен

### Шаг 2: Настройте
```bash
# Скопируйте пример конфига
cp env.example .env

# Отредактируйте .env и вставьте токен
nano .env
```

### Шаг 3: Запустите
```bash
# Установите зависимости
pip install -r requirements.txt

# Запустите бота
python bot.py
```

## 🔍 Проверка

Откройте бота в Telegram и отправьте:
- `/start` → должны увидеть картинку и кнопку
- `/who` → `ceo of everything @jypixob`
- `/fuck` → `fuck corporations 🖕`

## 📖 Детальные инструкции

1. **Быстрый старт** → смотрите [QUICKSTART.md](QUICKSTART.md)
2. **Подробная установка** → смотрите [SETUP.md](SETUP.md)
3. **Настройка команд** → смотрите [BOT_COMMANDS.md](BOT_COMMANDS.md)

## 🌐 Деплой игры

Чтобы кнопка "🎮 Играть" работала:

1. Задеплойте игру на [Render.com](https://render.com):
   - Создайте **Static Site**
   - Подключите GitHub репозиторий
   - Скопируйте URL

2. Обновите `.env`:
   ```
   WEBAPP_URL=https://ваш-адрес.onrender.com
   ```

3. Настройте Menu Button в @BotFather:
   - `/mybots` → ваш бот
   - **Bot Settings** → **Menu Button**
   - Введите URL с Render

## 💡 Полезные команды

```bash
# Тест конфигурации
python test_bot.py

# Запуск с помощью скрипта
./start_bot.sh

# Запуск в фоне (Linux/Mac)
nohup python bot.py > bot.log 2>&1 &

# Просмотр логов
tail -f bot.log
```

## 🎨 Кастомизация

Хотите изменить тексты? Откройте `bot.py` и отредактируйте:

```python
# Приветственный текст (строка ~27)
welcome_text = (
    "🥷 <b>Офисный ниндзя</b>\n\n"
    "Ваш текст здесь..."
)

# Команда /who (строка ~71)
await update.message.reply_text("Ваш текст")

# Команда /fuck (строка ~82)
await update.message.reply_text("Ваш текст")
```

## ❓ Проблемы?

### Бот не запускается
```bash
# Проверьте зависимости
pip install -r requirements.txt

# Проверьте конфигурацию
python test_bot.py
```

### Токен не работает
- Убедитесь, что скопировали полный токен из @BotFather
- Проверьте, что в `.env` нет лишних пробелов
- Токен должен быть в формате: `1234567890:ABCdef...`

### Картинка не показывается
- Убедитесь, что файл `images/start.jpg` существует
- Проверьте права доступа к файлу

## 🎉 Готово!

Теперь у вас есть полноценный Telegram бот с мини-приложением! 

**Вопросы?** Смотрите документацию или пишите @jypixob

---

**Автор бота**: @jypixob  
**Проект**: Офисный ниндзя 🥷

